[package]
name = "bot"
version = "0.1.0"
edition = "2024"

[dependencies]
poise = "0.6.1"
push-receiver = { path = "../push-receiver" }
serenity = { version = "0.12.5", features = ["client", "gateway", "model", "cache"] }
tokio.workspace = true
